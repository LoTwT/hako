[package]
name = "hako"
version = "0.0.0"
edition = "2021"

[lib]
name = "hako_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
